[project]
name = "code-review"
version = "0.1.0"

dependencies = [
    "fastapi==0.111.0",
    "uvicorn==0.30.1",
    "pydantic==2.7.1",
    "openenv==0.1.13"
]

[project.scripts]
server = "server.app:main"